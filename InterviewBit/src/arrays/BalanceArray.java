package arrays;

public class BalanceArray {
	public static void main(String[] args) {
		int[] inputArray = new int[] { 5, 5, 2, 5, 8 };
		// finding special elements in an array , an array is said to be special if
		// removal of that element
		// from array makes an array balanced and array is said to be balanced if
		// sum of even index element is equal to sum of odd index element

		int sumOdd = 0;
		int count = 0;
		for (int i = 0; i < inputArray.length; i++) {
			inputArray[i] = 0;
			for (int j = 0; j < inputArray.length; j++) {
				
				
			}
		}
	}
	
}
